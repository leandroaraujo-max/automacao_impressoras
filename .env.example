# .env.example — Modelo de configuração (versionar este arquivo)
# Copie para .env e preencha com seus valores reais.
# O arquivo .env nunca deve ser comitado (ver .gitignore).

# Porta do servidor Flask
PROBE_PORT=8080

# Comunidade SNMP (leitura)
SNMP_COMMUNITY=public

# Timeouts em segundos
HTTP_TIMEOUT=3
SNMP_TIMEOUT=2

# Número máximo de scans nmap paralelos
MAX_CONCURRENT=10

# Credenciais HP EWS (usuários e senhas separados por vírgula)
HP_EWS_USERS=administrador,admin
HP_EWS_PASSWORDS=SENHA1,SENHA2,

# Credenciais Samsung SyncThru
SAMSUNG_USER=admin
SAMSUNG_PASSWORDS=SENHA1,SENHA2,

# Credenciais Zebra ZebraNet
ZEBRA_USER=admin
ZEBRA_PASSWORDS=SENHA1,SENHA2,

# ---------------------------------------------------------------------------
# Autenticação LDAP / Active Directory (opcional)
# Deixe AD_SERVER em branco para usar somente o admin.key local.
# ---------------------------------------------------------------------------

# Endereço do servidor AD (ex: ldap://10.0.0.1 ou ldaps://dc.corp.local)
AD_SERVER=

# Domínio AD — usado para montar o UPN (usuario@dominio)
AD_DOMAIN=corp.local

# Base DN para pesquisa de grupo (ex: DC=corp,DC=local)
AD_BASE_DN=DC=corp,DC=local

# DN do grupo de administradores de impressoras (opcional — se vazio, qualquer login válido no AD é aceito)
AD_ADMIN_GROUP=CN=TI-Impressoras,OU=Grupos,DC=corp,DC=local

# Usar SSL/LDAPS (true/false)
AD_USE_SSL=false

# Timeout de conexão LDAP em segundos
AD_TIMEOUT=5
